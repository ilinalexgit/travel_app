'use strict';

angular
    .module('travelApp')
    .controller('SignupController', ['$location', '$rootScope',
        function ($location, $rootScope) {
            console.log('signup controller');
        }
    ]);
